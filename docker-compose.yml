version: '3.9'

services:
  database:
    image: postgres:14-alpine
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=nicu_op
    ports:
      - "5432:5432"